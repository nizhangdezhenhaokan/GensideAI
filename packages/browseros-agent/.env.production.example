<<<<<<< HEAD
```env
# 由 bun run env:examples 自动生成，请勿手动修改。
# 这是 BrowserOS Monorepo 根目录使用的生产环境变量示例文件。
# 使用时复制为 .env.production；真正的 .env.production 已被 Git 忽略，不会提交。


# --- Claw / BrowserOS neo ---

# Claw Server 使用的 PostHog 项目 Key。
# Production 构建时必须配置。
CLAW_POSTHOG_KEY=

# 可选：Claw Server 使用的 PostHog 服务地址。
# 默认使用 PostHog US Cloud。
# CLAW_POSTHOG_HOST=https://us.i.posthog.com

# BrowserClaw 客户端 Bundle 中嵌入的 PostHog 项目 Key。
# Production 构建时必须配置。
VITE_CLAW_POSTHOG_KEY=

# 可选：BrowserClaw 前端 Bundle 使用的 PostHog 服务地址。
# 默认使用 PostHog US Cloud。
# VITE_CLAW_POSTHOG_HOST=https://us.i.posthog.com


# --- BrowserOS Server ---

# BrowserOS Server 配置服务 URL。
# Production 构建时必须配置。
BROWSEROS_CONFIG_URL=https://llm.browseros.com/api/browseros-server/config

# BrowserOS Server Telemetry / PostHog Key。
# CLI Release 构建也会读取同一个 Key。
POSTHOG_API_KEY=

# BrowserOS Server 使用的 Sentry DSN。
# 用于服务端异常和错误上报。
SENTRY_DSN=

# Server 和构建脚本运行环境。
# production 表示生产环境。
NODE_ENV=production

# BrowserOS Server 日志级别。
LOG_LEVEL=info


# --- 构建产物上传 ---

# Cloudflare R2 账户 ID。
# 用于上传正式发布的构建产物。
R2_ACCOUNT_ID=

# Cloudflare R2 Access Key ID。
# 用于生产构建产物上传认证。
R2_ACCESS_KEY_ID=

# Cloudflare R2 Secret Access Key。
# 用于生产构建产物上传认证。
R2_SECRET_ACCESS_KEY=

# Cloudflare R2 存储桶名称。
# 正式构建产物会上传到该 Bucket。
R2_BUCKET=browseros


# --- Windows 代码签名 ---

# BrowserOS Windows 正式构建使用的 eSigner 用户名。
ESIGNER_USERNAME=

# BrowserOS Windows 正式构建使用的 eSigner 密码。
ESIGNER_PASSWORD=

# BrowserOS Windows 正式签名使用的 TOTP 二次验证密钥。
ESIGNER_TOTP_SECRET=

# BrowserOS Windows 正式签名使用的证书 / 凭据 ID。
ESIGNER_CREDENTIAL_ID=
```
=======
# Generated by bun run env:examples. Do not edit by hand.
# This is the production env example for the BrowserOS monorepo root.
# Copy it to .env.production; real .env.production files are gitignored.

# --- claw ---

# Claw server PostHog project key required by production builds.
CLAW_POSTHOG_KEY=

# Optional Claw server PostHog host; defaults to PostHog US Cloud.
# CLAW_POSTHOG_HOST=https://us.i.posthog.com

# BrowserClaw PostHog project key embedded in the shipped client bundle; required by production builds.
VITE_CLAW_POSTHOG_KEY=

# Optional BrowserClaw bundle PostHog host; defaults to PostHog US Cloud.
# VITE_CLAW_POSTHOG_HOST=https://us.i.posthog.com

# --- server ---

# BrowserOS server config URL required by production builds.
BROWSEROS_CONFIG_URL=https://llm.browseros.com/api/browseros-server/config

# Fixed vLLM OpenAI-compatible base URL, including the /v1 prefix.
# BROWSEROS_VLLM_BASE_URL=http://127.0.0.1:8000/v1

# Optional API key sent to the fixed vLLM OpenAI-compatible endpoint.
# BROWSEROS_VLLM_API_KEY=

# Fixed served model name exposed by the vLLM /v1/models endpoint.
# BROWSEROS_VLLM_MODEL=your-served-model-name

# Server telemetry key; CLI release builds read the same key.
POSTHOG_API_KEY=

# Server Sentry DSN.
SENTRY_DSN=

# Node environment for server and build scripts.
NODE_ENV=production

# Server log level.
LOG_LEVEL=info

# --- upload ---

# R2 account ID for production artifact uploads.
R2_ACCOUNT_ID=

# R2 access key ID for production artifact uploads.
R2_ACCESS_KEY_ID=

# R2 secret access key for production artifact uploads.
R2_SECRET_ACCESS_KEY=

# R2 bucket for production artifact uploads.
R2_BUCKET=browseros

# --- sign ---

# BrowserOS build eSigner username for Windows production signing.
ESIGNER_USERNAME=

# BrowserOS build eSigner password for Windows production signing.
ESIGNER_PASSWORD=

# BrowserOS build eSigner TOTP secret for Windows production signing.
ESIGNER_TOTP_SECRET=

# BrowserOS build eSigner credential ID for Windows production signing.
ESIGNER_CREDENTIAL_ID=
>>>>>>> GensideAI/lsk
